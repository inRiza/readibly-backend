[build]
builder = "nixpacks"
buildCommand = "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"

[build.environment]
NIXPACKS_PKGS = "portaudio portaudio-devel"
PYTHON_VERSION = "3.11" 